type Query {
    testQuery: QueryResult
}

type Mutation {
    update(id: String!): Update!
}

type Subscription {
    results: QueryResult
}

type QueryResult {
    id: String
}

type Update {
    id: String
}