# Simple TLS config file

jetstream = {
  store_dir = "/data/nats-server/"
}

tls {
  cert_file:  "/etc/nats/server-dev.pem"
  key_file:   "/etc/nats/server-dev-key.pem"
  timeout:    2

  # Optional certificate authority for clients
  ca_file:   "/etc/nats/intermediate_ca.pem"
}
