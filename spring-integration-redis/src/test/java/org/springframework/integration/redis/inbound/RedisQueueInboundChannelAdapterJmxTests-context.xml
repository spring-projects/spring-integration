<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:int="http://www.springframework.org/schema/integration"
	xmlns:util="http://www.springframework.org/schema/util"
	xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd">

	<context:mbean-server />
	<context:mbean-export />

	<int:poller id="redisPoller" fixed-rate="0" max-messages-per-poll="1"/>

	<bean id="redisConnectionFactory" class="org.springframework.data.redis.connection.jedis.JedisConnectionFactory">
		<property name="port" value="#{T(org.springframework.integration.redis.rules.RedisAvailableRule).REDIS_PORT}"/>
	</bean>

	<bean id="redisTemplate" class="org.springframework.data.redis.core.StringRedisTemplate">
		<property name="connectionFactory" ref="redisConnectionFactory"/>
	</bean>

	<bean id="myRedisQueueInboundMessageSource" class="org.springframework.integration.redis.inbound.RedisQueueMessageSource">
		<constructor-arg name="connectionFactory" ref="redisConnectionFactory"/>
		<constructor-arg name="queueName" value="jmxtests"/>
		<property name="messageReturned" value="false"/>
	</bean>

	<bean id="myRedisQueueInboundChannelAdapter" class="org.springframework.integration.config.SourcePollingChannelAdapterFactoryBean">
		<property name="source" ref="myRedisQueueInboundMessageSource"/>
		<property name="outputChannel" ref="outputChannel"/>
		<property name="pollerMetadata" ref="redisPoller"/>
		<property name="autoStartup" value="false"/>
	</bean>

	<int:channel id="outputChannel">
		<int:queue/>
	</int:channel>

</beans>
